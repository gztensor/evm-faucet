# Subtensor EVM Testnet Tokens Faucet

## Development

First, prepare an `.env` file. Copy `.env.dist` to `.env` and set up the wallet key.

Run `npm install` then `npm run dev`.

## Running in Docker

```bash
docker compose up -d --build
```
